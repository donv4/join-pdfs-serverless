---
// src/pages/pdf-to-excel.astro
import Layout from '../layouts/Layout.astro';
---
<Layout 
    title="PDF to Excel Converter - Extract Tables from PDF to Excel Online" 
    description="Extract tables from PDF files and convert them to Excel (.xlsx) format with multiple sheets. Free online conversion tool."
>
    <!-- Inject page-specific stylesheet from your public folder assets -->
    <link rel="stylesheet" href="/css/pdf_to_excel.css" slot="extra-head" />

    <div class="upload-container">
        <div class="upload-header">
            <h1>PDF to Excel Converter</h1>
            <p class="subtitle">Extract tables from PDF files and convert them to Excel (.xlsx) format with multiple sheets.</p>
        </div>

        <!-- Interactive Drag-and-Drop Area -->
        <div class="upload-area" id="uploadArea" style="cursor: pointer;">
            <i class="fas fa-file-excel upload-icon"></i>
            <h3>Drop your PDF file here</h3>
            <p class="upload-hint">or click to browse your computer</p>
            <input type="file" id="pdfFile" accept=".pdf" style="display: none;" />
            <button class="browse-btn" id="browseBtn">
                <i class="fas fa-folder-open"></i> Choose PDF File
            </button>
            <p class="file-info">Maximum file size: 50MB • Extracts tables to .xlsx format</p>
        </div>

        <div class="action-buttons">
            <button class="btn-primary" id="convertBtn">
                <i class="fas fa-sync-alt"></i> Extract Tables to Excel
            </button>
        </div>

        <!-- Progress Overlay (Initially Hidden) -->
        <div class="progress-overlay" id="progressOverlay" style="display: none;">
            <div class="progress-content">
                <div class="progress-spinner">
                    <i class="fas fa-spinner fa-spin"></i>
                </div>
                <h3>Processing PDF</h3>
                <p id="progressText">Extracting tables from PDF...</p>
                <div class="progress-bar">
                    <div class="progress-fill" id="progressFill"></div>
                </div>
            </div>
        </div>

        <!-- Success Overlay (Initially Hidden) -->
        <div class="success-overlay" id="successOverlay" style="display: none;">
            <div class="success-content">
                <div class="success-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <h3>Conversion Successful!</h3>
                <p>Tables have been extracted from your PDF and converted to Excel format.</p>
                <div class="success-stats" id="successStats">
                    <!-- Stats will be inserted here dynamically -->
                </div>
                <a href="#" class="btn-download" id="downloadBtn">
                    <i class="fas fa-download"></i> Download Excel File
                </a>
                <button class="btn-secondary" id="convertAnotherBtn">
                    <i class="fas fa-redo"></i> Convert Another PDF
                </button>
            </div>
        </div>

        <!-- Features Section -->
        <div class="features-section">
            <h3>Why Use Our PDF to Excel Converter?</h3>
            <div class="features-grid">
                <div class="feature">
                    <i class="fas fa-table"></i>
                    <h4>Table Extraction</h4>
                    <p>Automatically detects and extracts tables from your PDF documents.</p>
                </div>
                <div class="feature">
                    <i class="fas fa-file-excel"></i>
                    <h4>Excel Format</h4>
                    <p>Creates properly formatted Excel files with separate sheets for each table.</p>
                </div>
                <div class="feature">
                    <i class="fas fa-columns"></i>
                    <h4>Multiple Sheets</h4>
                    <p>Organizes tables into separate Excel sheets for easy navigation.</p>
                </div>
                <div class="feature">
                    <i class="fas fa-shield-alt"></i>
                    <h4>Secure Processing</h4>
                    <p>Your files are processed securely inside your browser with complete privacy.</p>
                </div>
            </div>
        </div>

        <!-- PDF Requirements Note -->
        <div class="requirements-note" style="margin-top: 30px; padding: 15px; background: #fff3cd; border-left: 4px solid #ffc107; border-radius: 4px; text-align: left;">
            <h4 style="margin-top: 0; color: #856404; font-weight: bold;">
                <i class="fas fa-info-circle"></i> PDF Requirements for Best Results:
            </h4>
            <ul style="margin-bottom: 0; color: #856404; padding-left: 20px; line-height: 1.6;">
                <li><strong>Digital PDFs:</strong> Works best with PDFs containing selectable text (not scanned images).</li>
                <li><strong>Table Structure:</strong> Clear table borders or consistent spacing improves detection.</li>
                <li><strong>Scanned PDFs:</strong> Image-based PDFs are processed smoothly with underlying client canvas layers.</li>
                <li><strong>File Size:</strong> Up to 50MB, 100 pages maximum.</li>
            </ul>
        </div>
    </div>

    <!-- How It Works Flow -->
    <div class="how-it-works" style="margin-top: 40px; text-align: center;">
        <h3>How to Convert PDF to Excel</h3>
        <div class="steps" style="display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; margin-top: 20px;">
            <div class="step" style="flex: 1; min-width: 200px; max-width: 280px; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.02);">
                <div class="step-number" style="background: #4a6cf7; color: white; width: 30px; height: 30px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 15px; font-weight: bold;">1</div>
                <i class="fas fa-upload" style="font-size: 24px; color: #4a6cf7; margin-bottom: 10px;"></i>
                <h4 class="fw-bold">Upload PDF</h4>
                <p class="text-muted small">Select or drag & drop your PDF file containing tables.</p>
            </div>
            <div class="step" style="flex: 1; min-width: 200px; max-width: 280px; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.02);">
                <div class="step-number" style="background: #4a6cf7; color: white; width: 30px; height: 30px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 15px; font-weight: bold;">2</div>
                <i class="fas fa-cogs" style="font-size: 24px; color: #4a6cf7; margin-bottom: 10px;"></i>
                <h4 class="fw-bold">Extract Tables</h4>
                <p class="text-muted small">Our tool automatically detects and isolates text matrices.</p>
            </div>
            <div class="step" style="flex: 1; min-width: 200px; max-width: 280px; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.02);">
                <div class="step-number" style="background: #4a6cf7; color: white; width: 30px; height: 30px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 15px; font-weight: bold;">3</div>
                <i class="fas fa-download" style="font-size: 24px; color: #4a6cf7; margin-bottom: 10px;"></i>
                <h4 class="fw-bold">Download Excel</h4>
                <p class="text-muted small">Download your clean spreadsheet layout immediately.</p>
            </div>
        </div>
    </div>

    <!-- Upgrade Marketing CTA Section -->
    <div class="upgrade-cta" style="margin: 40px auto; max-width: 1000px; padding: 25px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 12px; color: white; text-align: left;">
        <div style="display: flex; align-items: center; gap: 20px; flex-wrap: wrap;">
            <div style="flex: 1; min-width: 280px;">
                <h3 style="color: white; margin: 0 0 10px 0;">Need More Than Conversion?</h3>
                <p style="margin: 0; opacity: 0.9;">Our free tool extracts your data matrices securely, but what if you need to:</p>
                <ul style="margin: 10px 0 0 0; padding-left: 20px;">
                    <li>Edit text directly in cell layers?</li>
                    <li>Add corporate signatures or annotations?</li>
                    <li>Convert flattened image archives with deep OCR?</li>
                    <li>Process 100+ document sheets at once?</li>
                </ul>
            </div>
            <div style="text-align: center; min-width: 200px; margin-top: 10px;">
                <a href="/pdf-comparison" style="display: inline-block; background: white; color: #667eea; padding: 12px 30px; border-radius: 30px; text-decoration: none; font-weight: bold; transition: all 0.3s;">
                    Compare Premium Solutions →
                </a>
                <p style="margin: 10px 0 0 0; font-size: 14px; opacity: 0.8;">Find the right tool for advanced needs</p>
            </div>
        </div>
    </div>
</Layout>

<!-- Client side interaction logic controllers -->
<script is:inline src="/js/pdf_to_excel.js"></script>

<!-- Inline script link fallback for Browse triggering conformity -->
<script is:inline>
document.addEventListener('DOMContentLoaded', () => {
    const uploadArea = document.getElementById('uploadArea');
    const pdfFile = document.getElementById('pdfFile');
    const browseBtn = document.getElementById('browseBtn');

    if (uploadArea && pdfFile) {
        uploadArea.addEventListener('click', (e) => {
            if (e.target !== browseBtn && !browseBtn.contains(e.target)) {
                pdfFile.click();
            }
        });
    }
    if (browseBtn && pdfFile) {
        browseBtn.addEventListener('click', () => pdfFile.click());
    }
});
</script>
